export interface signUpFormType {
  login_id: string;
  password: string;
  name: string;
  nickname: string;
  email: string;
  department_id: number;
}

export interface loginData {
  username: string;
  password: string;
}
